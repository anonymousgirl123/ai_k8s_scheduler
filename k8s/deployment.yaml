
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-scheduler
  template:
    metadata:
      labels:
        app: ai-scheduler
    spec:
      containers:
      - name: scheduler
        image: ai-scheduler:latest
